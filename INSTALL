Requirements: 
 - MPS 3.0 (http://www.jetbrains.com/mps/download/)
 - Maven 

1. Initialize git submodules

   git submodule init
   git submodule update

2. Compile the required project files

   mvn clean compile

3. Launch MPS and open the project

4. Build the project with MPS

